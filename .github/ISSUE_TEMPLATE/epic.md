---
name: Epic
about: A body of work split across several issues
title: ''
labels: ''
---

## What this is

The problem in a few sentences, and why it needs more than one issue.

## Boundary

What is in scope and what is explicitly not. State it as a test where possible —
damson's existing one reads: *deleting `Sources/DamsonAgents` must leave damson
compiling, with `spawn-pane` and pane ids still useful for any argv.* A boundary you
can run beats a boundary you can argue about.

## Constraints found in the code

Traps confirmed in the repo before any code is written, with `file.swift:line`.
These are the reason the obvious approach is usually wrong.

## Sub-issues

- [ ] #N …
